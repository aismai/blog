<div>
    <h5 style="color: red">add permissions first</h5>
    <p>
        code: canEditAllBlogs, canDeleteAllBlogs
    </p>
    {{link-to 'add permissions' 'permissions.new' class='ui button'}}
</div>
<br>
{{link-to 'new role' 'roles.new' class='ui button'}}
<table class="ui celled table">
    <thead>
    <tr>
        <th>name</th>
        <th colspan="3">permissions</th>
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    {{#each model as |role|}}
        <tr>
            <td>{{link-to role.name 'permissions' role.id}}</td>
            <td colspan="3">
                {{#each role.permissions as |permission| }}
                    {{permission.code}}
                {{/each}}
            </td>
            <td>
                <button {{action 'deleteRole' role}}>
                    delete
                </button>
            </td>
        </tr>
    {{/each}}
    </tbody>
</table>