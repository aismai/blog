{{link-to 'new role' 'roles.new'}}

<table>
    <tr>
        <th>name</th>
        <th colspan="3">permissions</th>
        <th>actions</th>
    </tr>
    {{#each model as |role|}}
        <tr>
            <td>{{role.name}}</td>
            <td colspan="3">
                {{#each role.permissions as |permission| }}
                    {{permission.name}}
                {{/each}}
            </td>
            <td>
                <button {{action 'deleteRole' role}}>
                    delete
                </button>
            </td>
        </tr>
    {{/each}}
</table>